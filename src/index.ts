import {
	imgWaterMarker as marker,
	mosaic,
	blur,
	QRcode,
	fullMarker,
	rotate,
	filter,
} from './core/index'

export { marker, mosaic, blur, QRcode, fullMarker, rotate, filter }

export default { marker, mosaic, blur, QRcode, fullMarker, rotate, filter }
