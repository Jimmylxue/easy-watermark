import {
	imgWaterMarker as marker,
	mosaic,
	blur,
	QRcode,
	fullMarker,
	rotate,
} from './core/index'

export { marker, mosaic, blur, QRcode, fullMarker, rotate }

export default { marker, mosaic, blur, QRcode, fullMarker, rotate }
